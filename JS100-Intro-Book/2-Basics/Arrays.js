let listOfNum = [1, 2, 3, 4, 5];


let firstEl = listOfNum[0];
let secEl = listOfNum[1];

console.log(`The first element of the Array is: ${firstEl}`);
console.log(`The second element of the Array is: ${secEl}`);